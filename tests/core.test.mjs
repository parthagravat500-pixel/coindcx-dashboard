import test from "node:test";
import assert from "node:assert/strict";
import {mkdtempSync,rmSync} from "node:fs";
import {tmpdir} from "node:os";
import {join} from "node:path";
import {DEFAULT_SETTINGS,sizeTrade,exitPrice,signal,validSettings,utcDayKeys} from "../lib/strategy.ts";
import {initialState,advancePaper,closePaper,riskCheck,liveGates} from "../lib/paper.ts";
import {normalizeCandles,signedRead} from "../lib/exchange.ts";
import {parseFeed,newsBlocks} from "../lib/news.ts";
import {Store} from "../engine/store.mjs";
const inst={step:.001,tick:.01,minimum:.001,minNotional:5,maxQuantity:1000,feeRate:.00075,contractValue:1,active:true};
function trade(extra={}){return {id:"t",pair:"B-BTC_USDT",side:"LONG",quantity:1,entry:100,stop:95,target:109,openedAt:0,closedAt:undefined,pnl:0,fees:.075,funding:0,plannedRisk:5,status:"OPEN",lastBarTime:0,lastFundingAt:0,strategy:"test",...extra};}
test("position sizing respects the total budget including conservative costs",()=>{const r=sizeTrade(1000,100,98,inst,DEFAULT_SETTINGS,1000);assert.ok(r.quantity>0);assert.ok(r.risk<=2.5);assert.ok(r.quantity*2<2.5);});
test("minimum notional never forces an oversized trade",()=>{const r=sizeTrade(10,60000,59000,{...inst,step:.001,minimum:.001,minNotional:50},DEFAULT_SETTINGS,10);assert.equal(r.quantity,0);});
test("leverage does not multiply the loss budget",()=>{const a=sizeTrade(1000,100,98,inst,{...DEFAULT_SETTINGS,leverage:1},1000);const b=sizeTrade(1000,100,98,inst,{...DEFAULT_SETTINGS,leverage:3},1000);assert.equal(a.risk,b.risk);});
test("invalid sizing is rejected",()=>{for(const entry of [0,NaN,Infinity,-1])assert.equal(sizeTrade(1000,entry,98,inst,DEFAULT_SETTINGS,1000).quantity,0);assert.equal(sizeTrade(1000,100,98,inst,{...DEFAULT_SETTINGS,riskPct:5},1000).quantity,0);});
test("both stop and target touched resolves conservatively to the stop",()=>{const ex=exitPrice(trade(),{time:60000,open:100,high:111,low:94,close:105,volume:10},5);assert.equal(ex.reason,"Stop first (ambiguous candle)");assert.ok(ex.price<95);});
test("a gap through a stop fills at the worse open plus slippage",()=>{assert.ok(exitPrice(trade(),{time:60000,open:90,high:94,low:89,close:92,volume:10},5).price<90);});
test("short stop logic is symmetric",()=>{assert.ok(exitPrice(trade({side:"SHORT",stop:105,target:91}),{time:60000,open:110,high:112,low:100,close:106,volume:10},5).price>110);});
test("unfinished candles do not change the strategy's current decision",()=>{const now=300*60000;const c=Array.from({length:300},(_,i)=>({time:i*60000,open:100,high:101,low:99,close:100,volume:10}));const a=signal(c,now);const b=signal([...c,{time:now,open:100,high:10000,low:1,close:9000,volume:1000000}],now);assert.deepEqual(a,b);});
test("candle gaps block new signals",()=>{const c=Array.from({length:250},(_,i)=>({time:(i+(i>100?1:0))*60000,open:100,high:101,low:99,close:100,volume:10}));assert.match(signal(c,252*60000).reason,/gap/);});
test("malformed candles are discarded and duplicates are unique",()=>{const row={time:1700000040000,open:100,high:102,low:99,close:101,volume:20};assert.equal(normalizeCandles({data:[row,row,{...row,high:90}]}).length,1);});
test("paper closing accounts for entry and exit fees exactly once",()=>{const s=initialState();const t=trade({openedAt:Date.now(),fees:.075});s.portfolio.balance=999.925;s.portfolio.trades=[t];closePaper(s,t,110,"Test");assert.equal(Number(t.pnl.toFixed(4)),9.8425);assert.equal(Number(s.portfolio.balance.toFixed(4)),1009.8425);const balance=s.portfolio.balance;closePaper(s,t,120,"Repeated");assert.equal(s.portfolio.balance,balance);});
test("drawdown halts persist across a date boundary and reload",()=>{const now=Date.now(),s=initialState(now);s.portfolio.balance=900;riskCheck(s,now);assert.ok(s.portfolio.halted);const restored=JSON.parse(JSON.stringify(s));riskCheck(restored,now+86400000);assert.ok(restored.portfolio.halted);assert.equal(restored.settings.running,false);});
test("paper mode has no exchange-order API dependency",()=>{const s=initialState();s.settings.running=true;advancePaper(s);assert.equal(s.portfolio.trades.length,0);assert.equal(liveGates(s).every(g=>g.pass),false);});
test("same signal candle cannot create a duplicate entry",()=>{const now=Date.now(),s=initialState(now);s.settings.running=true;s.settings.newsGuard=false;const m={pair:"B-BTC_USDT",symbol:"BTC",price:100,bid:99.99,ask:100.01,spreadBps:2,turnover:1000000,change:1,funding:0,updatedAt:now,history:[],instrument:inst,signal:{action:"LONG",entry:100,stop:98,target:103.6,strategy:"test",regime:"Uptrend",reason:"test",barTime:Math.floor(now/60000)*60000-60000}};s.markets=[m];advancePaper(s,now);advancePaper(s,now+1000);assert.equal(s.portfolio.trades.length,1);});
test("stale quotes cannot open a position",()=>{const now=Date.now(),s=initialState(now);s.settings.running=true;s.settings.newsGuard=false;s.markets=[{pair:"B-BTC_USDT",price:100,bid:99.99,ask:100.01,spreadBps:2,updatedAt:now-60000,history:[],instrument:inst,signal:{action:"LONG",entry:100,stop:98,target:103.6,strategy:"test",regime:"Uptrend",reason:"test",barTime:now-60000}}];advancePaper(s,now);assert.equal(s.portfolio.trades.length,0);assert.match(s.portfolio.events[0].message,/stale/);});
test("risk settings reject unsafe and nonnumeric values",()=>{assert.throws(()=>validSettings({leverage:20}));assert.throws(()=>validSettings({riskPct:NaN}));assert.throws(()=>validSettings({maxPositions:5}));assert.throws(()=>validSettings({dailyLimitPct:10}));});
test("India day boundary and Monday week are explicit",()=>{assert.equal(utcDayKeys(Date.parse("2026-09-25T18:30:00Z")).day,"2026-09-26");assert.equal(utcDayKeys(Date.parse("2026-09-27T18:30:00Z")).week,"2026-09-28");});
test("RSS strips markup, blocks unsafe links and rejects future or undated stories",()=>{const now=Date.parse("2026-09-26T00:00:00Z"),date=new Date(now-1000).toUTCString(),xml="<rss><item><title><![CDATA[BTC &amp; network outage]]></title><link>https://example.org/news</link><pubDate>"+date+"</pubDate></item><item><title>Bad</title><link>javascript:alert(1)</link><pubDate>"+date+"</pubDate></item><item><title>Undated</title><link>https://example.org/x</link></item></rss>";const news=parseFeed(xml,"Official",true,now);assert.equal(news.length,1);assert.equal(news[0].title,"BTC & network outage");assert.equal(news[0].assets[0],"BTC");assert.equal(newsBlocks(news,now),true);assert.equal(newsBlocks(news.map(n=>({...n,verified:false})),now),false);});
test("read-only adapter cannot send orders",async()=>{await assert.rejects(()=>signedRead("/exchange/v1/derivatives/futures/orders/create","fake","fake"),/Read-only/);});
test("durable state and encrypted credentials survive restart",()=>{const dir=mkdtempSync(join(tmpdir(),"dcx-test-")),key="12".repeat(32);try{const a=new Store(dir,key);a.set("desk",{halted:"Loss limit",balance:90});const encrypted=a.encrypt({key:"private-key",secret:"private-secret"});assert.ok(!JSON.stringify(encrypted).includes("private-secret"));a.set("credentials",encrypted);a.put({id:"intent-1",state:"UNKNOWN",plan:{pair:"B-BTC_USDT"}});a.close();const b=new Store(dir,key);assert.equal(b.get("desk").halted,"Loss limit");assert.equal(b.credentials().secret,"private-secret");assert.equal(b.unresolved().length,1);b.close();}finally{rmSync(dir,{recursive:true,force:true});}});
